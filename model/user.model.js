// 'use strict';

// const mongoose = require('mongoose');
// const {Cases} = require('./cases.model');

// const CasesSchema = new mongoose.Schema ({
//     Country:{type:String},
//      confirmed:[CasesSchema],
//      recovered :[CasesSchema],
//      deaths: [CasesSchema],
//      capital_city:[CasesSchema],
//      updated: [CasesSchema]
//  })

// const casesModel = mongoose.model('covidCases',CasesSchema);

// module.exports=casesModel;